const REQUIRED = "tidak boleh kosong";
const uuidRequired = `uuid ${REQUIRED}`;
const faskesUuidRequired = `faskesUuid ${REQUIRED}`;
const codeRequired = `code ${REQUIRED}`;
const nameRequired = `name ${REQUIRED}`;
const addressUuidRequired = `addressUuid ${REQUIRED}`;
const phoneRequired = `phone ${REQUIRED}`;
const emailRequired = `email ${REQUIRED}`;
const websiteRequired = `website ${REQUIRED}`;
const urlGmapsRequired = `urlGmaps ${REQUIRED}`;
const photoRequired = `photo ${REQUIRED}`;
const clientSecretRequired = `client_secret ${REQUIRED}`;
const organizationIdRequired = `organization_id ${REQUIRED}`;
const clientIdRequired = `client_id ${REQUIRED}`;
const statusSatuSehatRequired = `status_satu_sehat ${REQUIRED}`;

export {
    uuidRequired,
    faskesUuidRequired,
    codeRequired,
    nameRequired,
    addressUuidRequired,
    phoneRequired,
    emailRequired,
    websiteRequired,
    urlGmapsRequired,
    photoRequired
};
